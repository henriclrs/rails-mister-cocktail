<div class="container">
  <div class="form-group">
    <div class="test">
      <%= simple_form_for @cocktail do |f| %>
        <%= f.input :name %>
        <%= f.submit "Validate", class: "btn btn-primary", id: "add-dose"%>
        <%= link_to '<i class="fas fa-undo-alt"></i> Back'.html_safe, cocktails_path, class: "btn btn-primary"%>
      <% end %>
    </div>
  </div>
</div>


